this folder contains 2 main file and a streamlit sim file in tournament traces folder
to run the main file  :
      install pytorch , transfomers
      pip install pytorch
      pip install transformers
      run the file - python present_main.py
      run the detector file - python present_detector.py --in output.txt
      run the simulator file in tournament traces folder streamlit run tournament_run.py
      
